{
  "name": "idleecology-builder-proxy",
  "version": "0.1.0",
  "description": "Local proxy for region builder to fetch Flora and eBird data (local dev only)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "node-fetch": "^2.6.7"
  }
}
